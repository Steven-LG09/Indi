import 'package:flutter/material.dart';

class OrnamentalScreen extends StatelessWidget {
  const OrnamentalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Esta es la pantalla de Uno'),
    );
  }
}
